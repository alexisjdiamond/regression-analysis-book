# LaTeX intermediates
*.aux
*.log
*.toc
*.idx
*.ind
*.ilg
*.out
*.synctex.gz
*.fls
*.fdb_latexmk

# The compiled PDF is a build product and is not stored in the repository at
# all. CI compiles it from the .tex source and publishes it to GitHub Pages and
# to Releases; `make book` writes local copies for previewing.
/regression_analysis_complete_final.pdf
/docs/regression_analysis_complete_final.pdf

# R intermediates
.Rhistory
.RData
.Rproj.user/
regression_companion_body.R
regression_companion_files/
# Root-level knit output is an intermediate; the docs/ copy is served by Pages.
/regression_companion.html

# OS noise
.DS_Store
Thumbs.db
